// comment
string name = "Ilya";
string surname;
integer age;
char firstLetterName;

age = 23;

sameName();

function sameName() {
    while (age < 18) {
        integer r = 1;
        if (name != "Ilya") {
            surname = "Baykalov";
            age = 23;
            age= age - 2 * 3;
        } else {
            surname = "NeBaykalov";
        }

        if (age == 23) {
            firstLetterName = 'I';
        }
    }

    return age;
}
